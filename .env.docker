# Docker Environment Configuration
# For VPS: Copy this to .env file and populate with your values
# Usage: docker-compose --env-file .env up -d

# ===== NEXT.JS APP CONFIGURATION =====
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://buyjan.com

# Directus API Configuration (internal Docker network URL)
# Use http://directus:8055 for inter-container communication
NEXT_PUBLIC_DIRECTUS_URL=http://directus:8055
DIRECTUS_API_TOKEN=your_directus_static_token_here
NEXT_PUBLIC_DIRECTUS_API_TOKEN=your_directus_static_token_here

# Optional Analytics
NEXT_PUBLIC_GA_ID=

# ===== DIRECTUS CMS CONFIGURATION =====
# Security Keys - Generate random values using: openssl rand -base64 32
DIRECTUS_KEY=your-random-key-here
DIRECTUS_SECRET=your-random-secret-here

# Database Configuration
DB_CLIENT=postgres
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=directus
DB_USER=directus
DB_PASSWORD=change_me_to_strong_password

# Directus Admin Credentials
ADMIN_EMAIL=admin@buyjan.com
ADMIN_PASSWORD=change_me_to_strong_password